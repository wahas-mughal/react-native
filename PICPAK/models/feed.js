class Feed {
    constructor(feedId, isLiked, title, profileDescription, profileImage){
        this.feedId = feedId,
        this.isLiked = isLiked,
        this.title = title,
        this.profileDescription = profileDescription,
        this.profileImage = profileImage
    }
}

export default Feed;