class Call {
  constructor(callId, callerName, caller , callTime) {
    (this.callId = callId),
      (this.callerName = callerName),
      (this.caller = caller),
      (this.callTime = callTime)
  }
}

export default Call;
