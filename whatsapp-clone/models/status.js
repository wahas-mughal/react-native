class Status {
  constructor(statusId, status, statusTitle, statusTime) {
    (this.statusId = statusId),
      (this.status = status),
      (this.statusTitle = statusTitle),
      (this.statusTime = statusTime);
  }
}

export default Status;
