class Chat {
  constructor(chatId, chatName, chatImage, chatMsgs, msgTime) {
    (this.chatId = chatId),
      (this.chatName = chatName),
      (this.chatImage = chatImage),
      (this.chatMsgs = chatMsgs);
      (this.msgTime = msgTime);
  }
}

export default Chat;
