class Details{
    constructor(place_id, name, photo_reference, rating){
        this.place_id = place_id,
        this.name = name,
        this.photo_reference = photo_reference,
        this.rating = rating
    }
}

export default Details;
