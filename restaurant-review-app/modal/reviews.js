class Reviews {
  constructor(name, profilePhoto, review, rating, reviewedAgo) {
    (this.name = name),
      (this.profilePhoto = profilePhoto),
      (this.review = review),
      (this.rating = rating),
      (this.reviewedAgo = reviewedAgo);
  }
}

export default Reviews;
